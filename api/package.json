{
  "name": "scheduler-api",
  "version": "0.1.0",
  "private": true,
  "description": "TypeSpec contract (single source of truth) for the meeting scheduler API",
  "scripts": {
    "build": "tsp compile .",
    "watch": "tsp compile . --watch"
  },
  "devDependencies": {
    "@stoplight/prism-cli": "^5.14.2",
    "@typespec/compiler": "latest",
    "@typespec/http": "latest",
    "@typespec/openapi": "latest",
    "@typespec/openapi3": "latest"
  }
}
